# EAS Build ignore file
# Same as .gitignore BUT without ios/ and android/ so EAS uploads the native projects

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native — EAS runs pod install, so Pods are not needed
ios/Pods/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env
.env*.local
.env.local

# typescript
*.tsbuildinfo

# Not needed for builds
.git/
.claude/
docs/
app-example
