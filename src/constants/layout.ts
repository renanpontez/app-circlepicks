/**
 * Layout constants shared across the app.
 * Keep in sync with src/app/(tabs)/_layout.tsx tab bar styles.
 */

export const TAB_BAR_HEIGHT = 80;
export const TAB_BAR_BOTTOM_SPACING = TAB_BAR_HEIGHT + 20;
