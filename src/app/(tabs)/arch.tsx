import { ArchScreen } from '@/presentation/screens';

/**
 * Rota Arquitetura (/arch)
 * Renderiza a ArchScreen seguindo o padrão MVVM
 */
export default function Arch() {
  return <ArchScreen />;
}
