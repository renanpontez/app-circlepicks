import { SettingsScreen } from '@/presentation/screens';

/**
 * Rota Configurações (/settings)
 * Renderiza a SettingsScreen seguindo o padrão MVVM
 */
export default function Settings() {
  return <SettingsScreen />;
}
